---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - svc.yaml
  - pvc-config.yaml
  - pvc-sitemap.yaml
  - deployment.yaml

generatorOptions:
  disableNameSuffixHash: true
